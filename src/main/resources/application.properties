# Application Name
spring.application.name=coffeeshop

# --- Database Connection (Using Placeholders for Render) ---
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# --- JPA/Hibernate Settings ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- JWT Properties (Hardcoded Secret is OK for this project) ---
app.jwtSecret=p2s5v8y/B?E(H+MbQeThWmZq4t7w9z$C&F)J@NcRfUjXn2r5u8x/A%D*G-KaPdSg
app.jwtExpirationInMs=86400000

# --- Redis Cache Configuration (Render doesn't have a free tier Redis) ---
# We will disable Redis for the Render deployment to stay on the free tier.
spring.cache.type=none